% table products

(* без указаний -- из базовой таблицы в модель: *)
% begin data all
    from products
   order by id
% end

% begin data single ~single:true
    from products
   where @id : id = id
% end
